# DATABASE CONFIGS

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=


# MAILER CONFIGS

EMAIL_SERVICE=gmail          # Optional: service name (gmail, outlook, etc.)
EMAIL_HOST=smtp.gmail.com    # SMTP server host
EMAIL_PORT=587               # SMTP port (defaults to 587 if not set)
EMAIL_USER=tu-email@gmail.com
EMAIL_PASSWORD=tu-contraseña-de-aplicación

# TRACCAR CONFIGS

NEXT_PUBLIC_TRACCAR_API_URL=http://localhost:8082/api    # Traccar API base URL
TRACCAR_API_USER=admin@traccar.org                       # Admin email for user creation (server-side only)
TRACCAR_API_PASSWORD=admin                               # Admin password for user creation (server-side only)
# Alternative names (for backward compatibility):
# TRACCAR_ADMIN_EMAIL=admin@traccar.org
# TRACCAR_ADMIN_PASSWORD=admin